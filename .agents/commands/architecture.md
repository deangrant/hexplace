# Architecture

Load skill `.agents/skills/hexplace-architecture` and root `AGENTS.md`.

Answer or plan using Hexplace crate boundaries, on-disk schema v2 layout, and
documented query paths. Do **not** invent out-of-scope features (planet
sharding, relation import / admin PIP, Nominatim display parity, minutely
diffs, house-number interpolation, learned importance).

Prefer linking to `.agents/docs/ARCHITECTURE.md` and `README.md` (setup/ops)
over duplicating long explanations. If the task needs import/lock/schema
detail, also load `.agents/skills/hexplace-import`.
